<%= form_for @question do |f| %>
	
	<%= f.label :title %>
	<%= f.text_field :title %>

	<%= f.label :body, "beschrijving" %>
	<%= f.text_area :body %>

	<%= f.submit 'stel vraag', data: { disable_with: 'Aan het versturen...' } %>

<% end %>	