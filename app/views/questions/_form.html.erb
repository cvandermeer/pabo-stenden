<%= form_for @question do |f| %>
	
	<%= f.text_field :title, :placeholder => "Vraag?" %>

	<%= f.text_area :body, :placeholder => "Beschrijving van de vraag" %>

	<%= f.submit 'Stel vraag', data: { disable_with: 'Aan het versturen...' }, :class => 'btn submit' %>

<% end %>	